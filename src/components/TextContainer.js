import React from 'react'

function TextContainer({users}) {
    console.log(users);
  return (
    <div>
        {/* {users.map((user) => {
            return (
                <div>
                    {user.name}
                </div>
            )
        })} */}
    </div>
  )
}

export default TextContainer